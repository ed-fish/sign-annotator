export const PLAYBACK_SPEEDS = [0.25, 0.5, 0.75, 1, 1.25, 1.5, 2, 4];

export const TIMELINE_MIN_ZOOM = 10; // px per second
export const TIMELINE_MAX_ZOOM = 1000; // px per second
export const TIMELINE_DEFAULT_ZOOM = 100; // px per second

export const MARKER_WIDTH = 2; // px
export const MARKER_HIT_WIDTH = 8; // px for click detection
export const TIER_HEIGHT = 36; // px
export const RULER_HEIGHT = 30; // px

export const MARKER_SHAPE_SIZE = 7; // px radius for marker shape at top
export const MINIMAP_HEIGHT = 14; // px for minimap bar at bottom
export const DOUBLE_CLICK_DELAY = 300; // ms to wait before treating as single-click
export const AUTO_SCROLL_SMOOTH_SPEED = 0.15; // lerp factor for smooth scroll

export const AUTOSAVE_DEBOUNCE_MS = 3000;
export const AUTOSAVE_INTERVAL_MS = 30000;
